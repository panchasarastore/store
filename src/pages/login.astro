---
// src/pages/login.astro
// Centralized login: Redirect to dashboard login
export const prerender = false;

const dashboardLoginUrl = "https://dashboard.pnsara.store/login";

return Astro.redirect(dashboardLoginUrl);
---
